package reference_resolution

import (
	"stencil/config"
)

func NeedToResolveRef(fromApp, toApp, toTable, toAttr string) {
	
}

func RefResolved(displayConfig *config.DisplayConfig, ) {

	// query := fmt.Sprintf("select pk, table_name from app_tables;")

}